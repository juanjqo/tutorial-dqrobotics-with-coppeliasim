[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "MyProject"
authors = [{name = "Juan Jose Quiroz Omana", email = "myemail.com"}]
dynamic = ["version", "description"]